use std::io::BufRead;

pub fn day5_part1() -> u64 {
    let reader = std::io::BufReader::new(std::fs::File::open("day-5.txt").unwrap());
    reader.lines().for_each(|line| {});
    0
}
pub fn day5_part2() -> u64 {
    0
}
